;; Exercise 1.34: Passing a function to itself (though not with useful results
;;                in this case)

;; The interpreter will understand the result to be (f 2), which is (2 2),
;; which will fail because 2 is not something to call.
